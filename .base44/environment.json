{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "WIX_CLIENT_ID",
      "description": "Wix OAuth App Client ID. Found in Wix Dashboard → OAuth Apps or via 'npx wix env pull'.",
      "requiredAtBoot": true
    },
    {
      "name": "WIX_CLIENT_SECRET",
      "description": "Wix OAuth App Client Secret. Found in Wix Dashboard → OAuth Apps or via 'npx wix env pull'.",
      "requiredAtBoot": true
    },
    {
      "name": "WIX_CLIENT_PUBLIC_KEY",
      "description": "Wix OAuth App Public Key. Found in Wix Dashboard → OAuth Apps or via 'npx wix env pull'.",
      "requiredAtBoot": true
    },
    {
      "name": "WIX_CLIENT_INSTANCE_ID",
      "description": "Wix App Instance ID. Found in Wix Dashboard → OAuth Apps or via 'npx wix env pull'.",
      "requiredAtBoot": true
    }
  ],
  "verifiedAtCommit": "d081f300d0c18fee0ed785d2732209f2bf31329f"
}
